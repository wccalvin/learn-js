# learn-js

## Style setting

```cmd
npm init
npm install eslint --save-dev
./node_modules/.bin/eslint --init
```

* Choose `airbnb` style.

* Add the following rules to `.eslintrc`

```json
"rules": {
    "no-console": "off"
}
```
